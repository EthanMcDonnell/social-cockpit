{
  "name": "social-cockpit-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server exposing social-cockpit's scheduling API (and read-only post analytics) to MCP clients.",
  "type": "module",
  "bin": {
    "social-cockpit-mcp": "./dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.9.3"
  }
}
